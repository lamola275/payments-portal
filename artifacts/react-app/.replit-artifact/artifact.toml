kind = "web"
previewPath = "/"
title = "React App"
version = "1.0.0"
id = "artifacts/react-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23965

[services.development]
run = "pnpm --filter @workspace/react-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/react-app", "run", "build" ]
publicDir = "artifacts/react-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23965"
BASE_PATH = "/"
